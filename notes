define - позволяет задать паттерн, для прижков через [d, [I и [i

include - тоже самое, но для import?

commentstring - шаблон комментария

dictionary - список файлов со словами для автозаполнения
diff - добавляет окно к окнам, которые показывают различие
diffexpr - кастомная функция сравнения, выдаёт формат unified diff


pa - список директорий, в который будет искать :find

vim.opt.suffixesadd = "" Это будет добавляться к слову под gf, чтобы найти файл

vim.opt.tags = "файлы с тегами"
var.opt.vartabstop = "4,4,8,4" -- 1 таб будет до 4, второй до 8, третий до 16, а остальные по +8
